import styled from "styled-components"

const RGToolip = (value: string) => {
    return (
        <Tooltip>
            {value}
        </Tooltip>
    )
}

const Tooltip = styled.div`
    
`

export default RGToolip